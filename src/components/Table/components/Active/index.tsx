import { StatusContainer, StatusIcon } from './styles';

export const ActiveComponentTable = () => {
  return (
    <StatusContainer>
      <StatusIcon />
      <p>Ativo</p>
    </StatusContainer>
  );
};